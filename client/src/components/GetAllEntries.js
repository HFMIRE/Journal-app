import React from "react";
import Card from "./Card";
const GetAllEntry = () => {
  return (
    <div>
      {/* need to map over card to display eacj entry */}
      <Card />
    </div>
  );
};

export default GetAllEntry;
